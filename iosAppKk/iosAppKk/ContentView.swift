import SwiftUI
import shared

struct ContentView: View {
    let greet = "Hello"
    
    var body: some View {
        Text("Hi there its KMM")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
